apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: todo-chatbot-config
spec:
  tracing:
    samplingRate: "1"
  metric:
    enabled: true
  logging:
    apiLogging:
      enabled: true
