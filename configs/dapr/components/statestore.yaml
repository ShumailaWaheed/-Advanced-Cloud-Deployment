apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.postgresql
  version: v1
  metadata:
  - name: connectionString
    value: "host=localhost user=postgres password=example dbname=todochatbot port=5432 sslmode=disable"
  - name: table
    value: "state"
  - name: actorStateStore
    value: "true"